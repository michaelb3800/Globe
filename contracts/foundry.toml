[profile.default]
src = "src"
out = "out"
libs = ["lib"]

[rpc_endpoints]
baseSepolia = "https://sepolia.base.org"

[etherscan]
baseSepolia = { key = "${BASESCAN_API_KEY}" }

[wallet]
from = "${PRIVATE_KEY}"
